# TODO List

- Support Cerebras API to use their free API resources: https://cloud.cerebras.ai/
- Discuss which models to test.
- Discuss whether we should pay some credits to some specific APIs (e.g., OpenAI).
- Investigate whether someone can allocate computation power in its own organization for open-source models.
- Improve the error handling (e.g., dynamic waiting time based on the API response).
- Check if MegaVul also includes pairs of (func_before, func_after) in which the func_before was modified, but was not vulnerable (i.e., unrelated to the CVE fix)
  